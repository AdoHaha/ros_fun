Solution versions for the behavior-tree exercises.

These notebooks mirror the student notebooks and add `Rozwiązanie` cells after
each task. They are kept in a hidden folder so they do not appear as the main
exercise path during class.
