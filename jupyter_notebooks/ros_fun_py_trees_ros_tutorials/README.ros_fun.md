This package is vendored from:

https://github.com/splintered-reality/ros_fun_py_trees_ros_tutorials

It is adapted for the `ros_fun` Jupyter workshop so the tutorial launch files
and console scripts install through the existing `ros_fun` ROS package. The
upstream BSD license is preserved in `LICENSE.upstream`.
